/**
 * CSS imports aren't executable in Jest. This stub is mapped by
 * `moduleNameMapper` in `jest.config.ts` so a module that does
 * `import "./foo.css"` resolves to an empty object.
 */
export default {};
