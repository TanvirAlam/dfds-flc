export { Button, type ButtonProps } from "@dfds-ui/navaigator";

export function buttonStyles(): string {
  // Kept for backward compatibility with existing code that uses this function
  // Actual styling is handled by the NavAIgator Button component
  return "";
}